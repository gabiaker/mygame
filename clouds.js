// console.log ('clouds.js is linked')

class Clouds {
    constructor() {
        this.gravity = 0.2;
        this.speed = 0;



    }
    
//     drawClouds() {
//         game.cloudImgs.forEach(function (elem) {
//             elem.x -= elem.speed;
      
//             //image(source, x coordinate, y coordinate, width, height)
//             image(elem.src, 0, elem.y, width, height);
//             image(elem.src, 0, elem.y + height, width, height);
      
//             if (elem.y <= -height) {
//               elem.y = 0;
//             }
//           });

//         image(this.game.cloudImgs, this.x, this.y, this.width, this.height)
//     }
// }
